<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.estagio">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET " />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_estagio"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_estagio_round"
        android:supportsRtl="true"
        >
        <activity android:name=".BemVindo"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <!---Inicializar pelo Bem Vindo-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Sobre"
            android:label="@string/title_activity_sobre"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Ajuda"
            android:label="@string/title_activity_ajuda"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Perfil"
            android:label="@string/title_activity_perfil"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".VisualizarVaga"
            android:label="@string/title_activity_visualizar_vaga"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Favoritos"
            android:label="@string/title_activity_favoritos"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".FiltrarVagas"
            android:label="@string/title_activity_filtrar_vagas"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Vagas"
            android:label="@string/title_activity_vagas"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AdicionarVaga"
            android:label="@string/title_activity_adicionar_vaga"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsFragment"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">


        </activity>
    </application>

</manifest>